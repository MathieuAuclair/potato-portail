@model IEnumerable<ApplicationPlanCadre.Models.CriterePerformance>

<table class="cool-table">
    <tr>
        <th>
            @Html.DisplayNameFor(m => m.numero)
        </th>
        <th>
            @Html.DisplayNameFor(m => m.description)
        </th>
        <th class="action-cell-header">
            <b>Modifier</b>
        </th>
        <th class="action-cell-header">
            <b>Supprimer</b>
        </th>
    </tr>

    @foreach (var item in Model)
    {
        string dblclickEvent = Url.Action("Modifier", new { idCritere = item.idCritere });
        <tr ondblclick="location.href='@dblclickEvent'">
            <td>
                @Html.DisplayFor(m => item.numero)
            </td>
            <td>
                @Html.DisplayFor(m => item.description)
            </td>
            <td class="action-cell">
                <a href='@Url.Action("Modifier", new { idCritere = item.idCritere })'>
                    <span class="glyphicon glyphicon-pencil"></span>
                </a>
            </td>
            <td class="action-cell">
                <a href='@Url.Action("Supression", new { idCritere = item.idCritere })'>
                    <span class="glyphicon glyphicon-trash"></span>
                </a>
            </td>
        </tr>
    }
</table>